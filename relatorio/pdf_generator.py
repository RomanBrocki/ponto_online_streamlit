# relatorio/pdf_generator.py
"""Geração do relatório de ponto em PDF, incluindo tabela e resumo."""

def gerar_relatorio_pdf(empregada: str, mes: str, dados: list[dict]) -> bytes:
    """
    Gera um PDF contendo os pontos registrados no mês e um resumo final.

    Args:
        empregada (str): Nome da empregada.
        mes (str): Mês no formato YYYY-MM.
        dados (list): Lista de registros com ponto do mês.

    Returns:
        bytes: Conteúdo binário do PDF.
    """
    pass
